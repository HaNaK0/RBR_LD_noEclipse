'''
Created on 12 aug 2014

@author: HaNaK0
'''
from tkinter import *
import widgets
import workspace as ws
import main

main = main.Main()
















#workspace = None #ws.Workspace(root, widgets.StatusBar(root, images), images, gridX = 32, gridY = 32, width = 1920 * 3, height = 1080)
#def newWorkSpace():
    #if workspace == None:
        #workspace = ws.Workspace(root, widgets.StatusBar(root, images), images, gridX = 32, gridY = 32, width = 1920 * 3, height = 1080)
        #root.config(menu = widgets.MenuBar(root, workspace, images, newWorkSpace))



#root = Tk()
#images = ws.loadImages()

#root.config(menu = widgets.MenuBarC(root, images, newWorkSpace))
#root.geometry("600x600")
#root.title("RBR_LD")
#root.mainloop()
